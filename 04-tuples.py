# Tuples - They are fixed lists.
# we define tuples in parantheses ().
# They can't be changed, added to or deleted.
# Once created they cannot be changed.

days = ('Mon','Tue','Wed','Thur','Fri','Sat','Sun')
print(days)
print(days[0])
print(days[-1])